# Build-time requirements. These are only needed on the machine that builds the
# site -- students need nothing but a web browser.
jupyterlite-core==0.8.3
jupyterlite-pyodide-kernel==0.8.6
jupyter-server>=2.14          # required for JupyterLite to index the contents/ folder
